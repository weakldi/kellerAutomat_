package automat;

import java.util.ArrayList;

public class Loader2 {
	public void load(String data){
		ArrayList<String> tokens = new ArrayList<>();
		for(String line:data.split("\n")){
			if(line.matches("Z\\{[A-Za-z,]*[A-Za-z]\\}")){
				
			}
		}
	}
}
